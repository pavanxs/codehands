# CodeHands Build Plan

The active build sequence, accepted contracts, deferred work, and unresolved decisions are maintained only in [`docs/CURRENT_PLAN.md`](./docs/CURRENT_PLAN.md).

This file is retained as a compatibility pointer. It must not contain a second copy of the implementation plan. Historical build-plan content remains available through Git history.
